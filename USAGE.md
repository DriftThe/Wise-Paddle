# Wise-Paddle · 使用文档

> 这份文档面向**使用者**——打开浏览器、上传文件、看结果、复制文字。
> 开发者文档请看 [README.md](README.md)。

---

## 0. 启动服务

如果你已经按 [README.md § 3-4](README.md#3-安装) 装好依赖、下载好模型，直接：

```bash
# Windows
.\.venv\Scripts\Activate.ps1
python app.py

# Linux / macOS
source .venv/bin/activate
python app.py
```

看到 `Uvicorn running on http://127.0.0.1:8000` 后，浏览器打开 <http://127.0.0.1:8000> 即可。

---

## 1. 界面总览

打开后你会看到 4 块区域：

```
┌──────────────────────────────────────────────────────────┐
│  Wise-Paddle · OCR Playground   pool 2/2  pending 0  batch 5  │  ← 顶部状态
├──────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────┐ │
│  │  把图片 / PDF 拖到这里，或者点我选择              │ │  ← 拖拽区
│  │  支持 PNG / JPG / WebP · PDF（多页自动拆任务）     │ │
│  └────────────────────────────────────────────────────┘ │
│                                                            │
│  队列 [3]                                                  │  ← 已加任务列表
│  ┌──┐ ┌──┐ ┌──┐                                           │
│  │图│ │图│ │PDF│   [开始处理] [清空队列]                    │
│  └──┘ └──┘ └──┘                                           │
│                                                            │
│  结果 [3 / 3]  ‹ [tab1] [tab2] [tab3] ›                   │  ← 多结果导航
│                                                            │
│  ┌─ result_1 ──────────────────────────────────────────┐  │
│  │ 📷 a.png        request_id 12ab · 1 页 · 完成  1.20s│  │  ← 当前结果卡
│  │ ┌──────────────────────────────────────────┐       │  │
│  │ │  [原图 + 检测框, 鼠标悬停高亮, 点击复制]  │       │  │
│  │ └──────────────────────────────────────────┘       │  │
│  │ [text]   0.97 · #0  "识别的文字..."                │  │  ← 区域卡片
│  │ [image]  0.95 · #1  (空)                          │  │
│  │ [table]  0.92 · #2  "| 表头 | 表内容 |"            │  │
│  └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

## 2. 上传文件

### 2.1 单图

**支持格式**：PNG / JPG / WebP（最大 32MB，可在环境变量调）

**操作**：拖到虚线框里，或点虚线框选文件。

- 单击多选用 `Ctrl/Shift`
- 上传后立即出现在"队列"区
- 可以一次性加多个，它们**不会自动开始处理**

### 2.2 PDF

**支持格式**：标准 PDF（最大 128MB，最多 200 页）

**操作**：
1. 拖入 PDF
2. 浏览器用 PDF.js 自动抽帧所有页面（CPU，几十毫秒~几秒）作为缩略图
3. 缩略图显示在"队列"区

> 缩略图只是**占位**，真正的 OCR 在你点"开始处理"后由服务端做。

### 2.3 混合

队列里**图片和 PDF 可以混着放**，点"开始处理"会一起跑。

## 3. 处理任务

### 3.1 启动

点 **"开始处理"** 按钮：

- 所有 `pending` 状态的任务进入 `processing`
- 结果区立即出现"占位卡"，带加载动画
- 任务完成（成功/失败）后卡升级为完成态

### 3.2 进度

**单图**：1 个占位卡 → 1 个结果卡

**PDF（多页）**：
- 1 个占位卡，**内含 N 个 page-slot**
- 每个 page-slot 状态独立：等待 → 处理中 → 完成
- 顶部 page chip 实时反映各页状态
- 一个页出错不影响其他页

### 3.3 排队

如果同时有多个用户/任务在跑：
- 顶部状态栏 `pool X/Y` 实时变化
- `pending` 数字告诉你还有多少没进 batch
- 批调度是**公平**的：长 PDF 不会独占，新来的小图会插队

## 4. 查看结果

### 4.1 多结果导航

当你有**多个结果**时，顶部出现一个**结果切换条**：

```
结果 3 / 3   ‹ [#3 report.pdf ✓] [#2 表格.png ✓] [#1 文档.png ✓] ›   3 / 3
```

- 缩略图来自原图（PDF 用第一页）
- 状态徽章：处理中（蓝）/完成（绿）/失败（红）
- 点缩略图：**滚动到对应结果** + 高亮闪烁
- 点 ‹ / ›：顺序切换

### 4.2 PDF 多页导航

结果卡顶部有一行 **page chip**（多页才有）：

```
‹ [1✓] [2●] [3✓] [4✓] [5✗] ›   3 / 5
```

- 点 chip：切换显示对应页
- ‹ / ›：顺序翻页
- 状态：✓ 完成 / ● 处理中 / ✗ 失败

### 4.3 查看检测框

结果卡里有原图 + 检测框叠加：
- **不同颜色代表不同类型**：
  - 🟢 绿色 = 文字块
  - 🟠 橙色 = 图片
  - 🟣 紫色 = 表格 / 公式
- **鼠标悬停**：高亮 + 显示标签
- **点击**：复制该块的文字到剪贴板

### 4.4 复制文字

两种方式：
- **点 canvas 上的检测框** → 复制
- **点结果卡下方的区域卡片** → 复制

成功后会有绿色短暂高亮 + 顶部 toast 提示"已复制 N 字符"。

## 5. 关页面 / 切换标签会发生什么

Wise-Paddle **不做任何"用户离线就自动取消任务"的逻辑**。所有任务一旦提交到服务端，就会老老实实跑完，关 tab / 切后台 / 网络断都不影响。

- **切到后台标签（窗口看不见）**：任务继续在后台跑；切回来页面就是最新的（不打断长任务）
- **关标签页 / 关浏览器**：服务端仍在跑，任务会跑完；下次开页面可以轮询 `/api/ocr/pdf-status/{user_id}` 看历史结果
- **服务端 shutdown**：仅在服务关闭时一次性取消所有未完成的 job
- **网络断 / 关 tab 期间 fetch 中断**：HTTP handler 收到 `CancelledError` 只把响应标记为 `cancelled` 返回 200，pending job **不会**被服务端自动取消（由调用方决定是否要外部清掉）

> 提示：如果你中途关 tab 又想拿到结果，**先等所有任务完成**再关；或者保留 user_id 后续轮询 PDF 进度。

## 6. 产物文件

每个任务完成后，对应一个 `text_result/{request_id}/` 目录：

```
text_result/
└── a1b2c3d4/                            ← 8 字符的 request_id
    ├── page_0_box_0_text.md             ← 第 0 页，第 0 块，文字
    ├── page_0_box_1_image.md            ← 第 0 页，第 1 块，图片
    ├── page_0_box_2_table.md            ← 第 0 页，第 2 块，表格
    ├── page_1_box_0_text.md
    └── ...
```

文件命名规则：`page_<page_idx>_box_<box_idx>_<label>.md`

同名（如同一页有多个 `text` 块）会自动加 `_1` `_2` 后缀。

> 💡 这些文件可以**直接用 markdown 编辑器 / VS Code 打开**，或者拼到知识库里。

## 7. 常见问题

### Q: 上传后没反应？
A: 检查浏览器 console（F12 → Console）。常见原因：
- 服务没起来 → 看终端
- CORS 跨域 → 检查 `CORS_ALLOW_ORIGINS` 配置
- 文件超过大小限制 → 看 toast 提示

### Q: OCR 结果跟图片对不上？
A: PaddleOCR-VL-1.6 在 transformers 5.x 下偶尔有幻觉。如果整段文字都错：
- 截图给我
- 同时附上 `logs/server.log` 里那条请求的 `[VL batch=N label=...]` 日志

### Q: PDF 大文件慢？
A: 单页平均 4-8 秒，20 页 PDF 大约 1-3 分钟。pipeline 池 2 路，2 个 PDF 同时跑也是 1-3 分钟。

### Q: 怎么批量处理？
A: 直接拖一堆文件进去，一起点"开始处理"。它们会跨用户/跨文件凑批。

### Q: 中文支持吗？
A: ✅ 完全支持。PaddleOCR-VL 训练时覆盖了 109 种语言。

### Q: 公式 / 手写体呢？
A: 公式识别依赖 PaddleOCR-VL 自身能力，对印刷体公式 OK。手写体识别率一般。

### Q: 怎么清空结果？
A: 页面最下面"清空结果"按钮，或者 `Ctrl+R` 刷新页面（会清空已展示的结果；服务端任务如果还在跑，刷新后可以用 user_id 通过 `/api/ocr/pdf-status/{user_id}` 继续轮询）。

## 8. 快捷键

| 键 | 作用 |
| -- | ---- |
| `Esc`  | 关闭 toast |
| `Ctrl+R`  | 刷新页面（**会清空所有结果**，慎用） |
| `F12` | 打开浏览器 devtools |

---

> 找不到答案？问开发者。开发者文档：[README.md](README.md)
